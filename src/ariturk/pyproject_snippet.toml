[project]
name = "ariturk"
version = "0.1.0"
description = "arı-türk: Turkish Text Cleaning & Normalization Library"
readme = "README.md"
license = {text = "Apache-2.0"}
requires-python = ">=3.10"
keywords = ["turkish", "cleaning", "normalization", "nlp", "preprocessing"]
dependencies = []

[project.optional-dependencies]
full = ["kokturk>=0.1.0"]
